# Required now: PostgreSQL connection for Prisma.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/unified_ops?schema=public"

# Required now for local dev auth mode.
AUTH_MODE="dev"
DEV_GUILD_SLUG="ems-reux-rp"

# Required later (Discord OAuth app-layer integration).
# AUTH_MODE="discord_oauth"
# DISCORD_CLIENT_ID=""
# DISCORD_CLIENT_SECRET=""
# DISCORD_REDIRECT_URI="http://localhost:3000/api/auth/discord/callback"

# Required later (web app -> bot sync orchestration boundary).
# DISCORD_SYNC_WEBHOOK_SECRET="replace-me"
# DISCORD_SYNC_BOT_API_BASE_URL="http://localhost:4000"
